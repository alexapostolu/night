name: C/C++ CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: windows-2019
    steps:
    - name: compiling for windows
      run: g++ src/night.cpp
    - uses: actions/checkout@v2
