print("Height: ");
height int = int(input());

for (i int = 1; i <= height; i += 1)
{
	for (j int = 0; j < height - i; j += 1)
		print(' ');

	for (j int = 0; j < i; j += 1)
		print('#');

	print("  ");

	for (j int = 0; j < i; j += 1)
		print('#');

	print('\n');
}