print("Height: ");
height int32 = int32(input());

for (i int32 = 1; i <= height; i += 1)
{
	for (j int32 = 0; j < height - i; j += 1)
		print(' ');

	for (j int32 = 0; j < i; j += 1)
		print('#');

	print("  ");

	for (j int32 = 0; j < i; j += 1)
		print('#');

	print('\n');
}