print("Shift: ");
shift int32 = int32(input());

print("Message: ");
message char[] = input();

for (i int32 = 0; i < len(message); i += 1)
{
	curr_char int32 = int32(message[i]) - int32('a');
	new_char int32 = (curr_char + shift) % 26;

	print(char(int32('a') + new_char));
}