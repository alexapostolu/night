print("Change Owned: ");
change int32 = int32(input());

while (change < 0)
{
	print("Change Owned: ");
	change = int32(input());
}

coins int32[] = [200, 100, 25, 10, 5, 1];

current_coin int32 = 0;
count int32 = 0;

while (change > 0)
{
	if (coins[current_coin] > change)
		current_coin += 1;
	else
	{
		change -= coins[current_coin];
		count += 1;
	}
}

print(count);
