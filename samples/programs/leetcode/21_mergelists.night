def merge_two_lists(l1 int32[], l2 int32[], n int32) char[]
{
	l3 char[] = "";
	j int32 = 0;

	for (i int32 = 0; i < n; i += 1)
	{
		while (l2[j] < l1[i])
		{
			l3 += str(l2[j]);
			j += 1;
		}

		l3 += str(l1[i]);
	}

	return l3;
}

print(merge_two_lists([1, 2, 4], [1, 3, 4], 3));
