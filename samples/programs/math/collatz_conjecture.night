def collatz(x int32) int32
{
	if (x % 2 == 0)
		return x/2;
	else
		return 3*x + 1;
}

print("Enter a number: ");
y int32 = collatz(int32(input()));

print(str(y) + "\n");

while (y != 1)
{
	y = collatz(y);
	print(str(y) + "\n");
}
